<resources>
    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#800000</item> <!-- Maroon -->
        <item name="colorPrimaryDark">#5C0000</item>
        <item name="colorAccent">#FFD700</item> <!-- Gold -->
    </style>

    <!-- Custom DatePicker Style -->
    <style name="CustomDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:colorAccent">@color/maroon</item>
    </style>

    <!-- Custom TimePicker Style -->
    <style name="CustomTimePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/white</item>
        <item name="android:colorAccent">@color/maroon</item>
    </style>

    <!-- Custom Button Style -->
    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/maroon</item> <!-- Maroon background -->
        <item name="android:textColor">@android:color/white</item> <!-- White text -->
        <item name="android:padding">10dp</item> <!-- Padding for buttons -->
    </style>
</resources>
